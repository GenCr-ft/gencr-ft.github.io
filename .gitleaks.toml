# gitleaks configuration — GenCr@ft Studio
# Ref: gcs-devops-standards/domains/security/.gitleaks.toml
[extend]
useDefault = true

[[allowlists]]
description = "Test fixtures and example values"
paths = [
    '''(^|/)tests?/''',
    '''(^|/)integration_test_docs/''',
    '''(^|/)test[-_]keys?/''',
    '''(^|/)fixtures?/''',
]
